//dummy export if this need to be made available to app.js it needs to be exported using export keyword, below is one way of exporting we have many
export let apikey = 'Testkey';

//second using default, and there should be only one default export in one file, and with default we dont need variable we can directly export value

export default "TestKeyDefaultExport";

export let multile = "abc";